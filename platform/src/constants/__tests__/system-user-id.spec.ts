import { SYSTEM_USER_ID } from '../system-user-id';

describe('constants', () => {
  it('should export SYSTEM_USER_ID with a value of "SYSTEM_USER_ID"', () => {
    expect(SYSTEM_USER_ID).toBe('SYSTEM_USER_ID');
  });
});
