import { useRetroHistory } from "../../../../../hooks/useRetroHistory";

// type Props ={
//     retros: 
// }

export const RetroSelector = () => {
//   const { retroList } = useRetroHistory(selectedTeam || "");

    return (  

        <p>slector loading</p>

    );
}
 